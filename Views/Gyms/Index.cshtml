@model IEnumerable<SoftwareIntegrationThirdVersion.Models.Gym>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.gymName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.gymAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.gymName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.gymAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.gymId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.gymId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.gymId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
